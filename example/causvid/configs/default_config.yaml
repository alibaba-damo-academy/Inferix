model_name: wan
generator_name: causal_wan
generator_ckpt: "wan_causal_ode_checkpoint_model_003000/model.pt"
generator_fsdp_wrap_strategy: size
real_score_fsdp_wrap_strategy: size
fake_score_fsdp_wrap_strategy: size
text_encoder_fsdp_wrap_strategy: size
generator_grad:
  model: true
real_score_grad:
  model: false
fake_score_grad:
  model: true
denoising_step_list:
- 1000
- 757
- 522
- 0
num_train_timestep: 1000
timestep_shift: 8.0
real_guidance_scale: 3.5
generator_task: causal_video
real_task_type: bidirectional_video
fake_task_type: bidirectional_video
denoising_loss_type: flow
mixed_precision: true
seed: 0
wandb_host: WANDB_HOST
wandb_key: WANDB_KEY
wandb_entity: tyin
wandb_project: causvid
wandb_name: wan_causal_dmd
sharding_strategy: hybrid_full
lr: 2.0e-06
beta1: 0.9
beta2: 0.999
data_path: mixkit_latents_lmdb
batch_size: 1
log_iters: 200
# negative_prompt: 'Vibrant colors, overexposed, static, blurry details, subtitles, style, artwork, scene, still, overall gray, worst quality, low quality, JPEG compression artifacts, ugly, incomplete, extra fingers, poorly drawn hands, poorly drawn face, malformed, disfigured, malformed limbs, fused fingers, still scene, cluttered background, three legs, many people in background, walking backwards'
negative_prompt: 'Vibrant colors, overexposed, static, blurry details, subtitles, style, artwork, scene, still, overall gray, worst quality, low quality, JPEG compression artifacts, ugly, incomplete, extra fingers, poorly drawn hands, poorly drawn face, malformed, disfigured, malformed limbs, fused fingers, still scene, cluttered background, three legs, many people in background, walking backwards'
dfake_gen_update_ratio: 5
image_or_video_shape:
- 1
- 21
- 16
- 60
- 104
output_path: /mnt/localssd/wan_causal_dmd
distillation_loss: dmd
gradient_checkpointing: true
backward_simulation: false
num_frame_per_block: 3
warp_denoising_step: false

# Memory optimization (framework-level)
# memory_mode: aggressive/balanced/relaxed (default: balanced)
# vae_chunk_size: override chunk size, null means use memory_mode preset
memory_mode: balanced
vae_chunk_size: null